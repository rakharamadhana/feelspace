{
  "name": "feelspace",
  "version": "1.0.0",
  "description": "Social Emotion Learning App from Digital Learning and Education, NTUST, Taiwan",
  "license": "MIT",
  "scripts": {
    "start": "concurrently \"yarn start-frontend\" \"yarn start-backend\"",
    "start-frontend": "yarn --cwd frontend start",
    "start-backend": "yarn --cwd backend start",
    "build": "yarn --cwd frontend build prod"
  },
  "dependencies": {
    "concurrently": "^6.2.1",
    "config": "^3.3.12",
    "dotenv": "^16.4.5"
  }
}
