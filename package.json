{
  "scripts": {
    "start": "concurrently \"yarn run start-frontend\" \"yarn run start-backend\"",
    "build": "yarn build prod --prefix frontend",
    "start-frontend": "yarn start --prefix frontend",
    "start-backend": "yarn start --prefix backend"
  },
  "dependencies": {
    "concurrently": "^6.2.1",
    "config": "^3.3.12",
    "dotenv": "^16.4.5"
  }
}
